
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csfaq.amazonassociatereporting"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="16" />

    <application android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar">

        <!-- Splash screen -->
        <activity
            android:name=".Activity.SplashScreenActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity android:name=".Activity.MainActivity" android:label="@string/app_name" />

        <activity android:name=".Activity.CollectionDemoActivity" android:label="@string/demo_collection" />

        <activity android:name=".Activity.NotificationReceiverActivity" android:label="@string/app_name" />
        
                <!-- Click View activity -->
        <activity android:name=".Activity.ClickViewActivity" android:label="@string/app_name" />


    </application>
</manifest>
